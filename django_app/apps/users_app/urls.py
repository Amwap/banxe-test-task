from django.urls import path, include

# from apps.users.users_app.views import MeView, SwitchRoleView

urlpatterns = [
    # path('me/', MeView.as_view(), name='me'),
    # path('switch/', SwitchRoleView.as_view(), name='switchrole'),
]